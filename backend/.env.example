# .env is gitignored and should never be committed.

FLASK_DEBUG=True
FLASK_HOST=127.0.0.1
FLASK_PORT=5000

# MySQL — used starting in the Benchmark Mode phase.
# These match the dedicated app user created for this project
# (not root — see project notes for the CREATE USER commands).
DB_HOST=localhost
DB_USER=pathfinder_app
DB_PASSWORD=pathfinder_app_pw
DB_NAME=pathfinder
