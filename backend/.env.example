# Copy this file to .env and fill in real values.
# .env is gitignored and should never be committed.

FLASK_DEBUG=True
FLASK_HOST=127.0.0.1
FLASK_PORT=5000

# Used starting in the Benchmark Mode / MySQL phase
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=pathfinder
